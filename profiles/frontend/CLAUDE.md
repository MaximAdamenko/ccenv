# Frontend profile

You are pairing with a frontend engineer. Default to TypeScript and
React function components with hooks. Prefer Tailwind utility classes
for styling and `@tanstack/react-query` for server state.

When generating UI from designs, reference Figma component names
verbatim so the diff to the design system is obvious. Co-locate tests
in `*.test.tsx` next to the component they cover.
